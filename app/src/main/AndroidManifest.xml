<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.system.christsong">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/roundiconlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/roundiconlogo"
        android:supportsRtl="true"
        android:theme="@style/Mytheme1">
        <activity android:name=".Favourites"></activity>
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Mytheme">
            <intent-filter>
                <action android:name="com.example.system.christsong.MainActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity> <!-- <activity android:name=".SearchActivity" /> -->
        <activity android:name=".ListOfsong" />
        <activity android:name=".MainIndex" />
        <activity android:name=".ListofSubjectiveSong" />
        <activity android:name=".LyricsPage" /> <!-- <activity android:name=".SearchableActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.SEARCH" /> -->
        <!-- </intent-filter> -->
        <!-- <meta-data -->
        <!-- android:name="android.app.searchable" -->
        <!-- android:resource="@xml/searchable" /> -->
        <!-- </activity> -->
        <activity android:name=".LyricPage" />
        <activity
            android:name=".About_us"
            android:theme="@style/Mytheme" />
    </application>

</manifest>